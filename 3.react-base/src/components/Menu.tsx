import React, { useCallback, useDebugValue, useEffect, useState } from "react";



const Menu  = () => {

    return (
        <div>
            Menu 
        </div>
    )
}

/**
 * React.memo est un HOC (Higher Order Component)
 * C-A-D une fonction prendant en éntrée un composant 
 * et retournant ce composant dans une logique contextuelle
 */
// export default React.memo(Menu);
export default Menu;